// "use client"
// import { create } from "zustand";

// interface Store{
//     theme: "dark" | "light";
//     setTheme: (theme: "dark" | "light") => void;
// }


// export const useThemeChange = create<Store>((set) => ({
//   theme: "dark",
//   setTheme: (theme) => set({ theme }),
// }));